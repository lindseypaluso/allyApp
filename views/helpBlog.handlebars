<div class="container">
  <div class="card bg-primary text-white text-center p-3">
    <h1 class="display-4">ALLY</h1>
    <blockquote class="blockquote mb-0">
      <h4 class="font-italic">noun:</h4>
      <p>One that is associated with another as a helper; a person or group that
        provides assistance and support in an ongoing effort, activity, or struggle.
      </p>
      <h4 class="font-italic">verb:</h4>
      <p>To unite or form a connection or relation between; associate.
      </p>
      <footer class="blockquote-footer text-white">
        <small>
          <cite title="Source Title">Merriam-Webster</cite>
        </small>
      </footer>
    </blockquote>
  </div>
</div>
<div class="container blog">
  <h1>Be an Ally</h1>
  <h3>Take action now!</h3>
  <div class="row">
    <div class="col-md-2 col-sm-2">
      <a class="btn btn-light" href="/form">Find an Ally</a>
    </div>
  </div>
  <div class="row">
    <div class="col-md-12 blog-container">
      {{#each data}}
      <div class="card">
        <div class="card-header">
          <button class="connect btn btn-info">Connect</button>
          <h4>Title: {{dataValues.title}}</h4>
          <p>Created: {{dataValues.createdAt}}</p>
          <p id="blogName">Username: {{dataValues.name}}</p>
        </div>
        <div class="card-body">
          <p>Description: {{dataValues.body}}</p>
        </div>
      </div>
      {{!-- <form action="/" method="POST">
        <textarea type="text" name="wish"></textarea>

        <button type="submit">Submit</button>
      </form> --}}
      {{/each}}
    </div>
  </div>
</div>
<script src="https://cdnjs.cloudflare.com/ajax/libs/moment.js/2.17.0/moment.min.js" type="text/javascript"></script>